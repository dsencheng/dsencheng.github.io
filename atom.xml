<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Dsen]]></title>
  <link href="https://dsencheng.github.io/atom.xml" rel="self"/>
  <link href="https://dsencheng.github.io/"/>
  <updated>2021-03-01T16:51:08+08:00</updated>
  <id>https://dsencheng.github.io/</id>
  <author>
    <name><![CDATA[]]></name>
    
  </author>
  <generator uri="http://www.mweb.im/">MWeb</generator>
  
  <entry>
    <title type="html"><![CDATA[超快！UIScrollView嵌套UICollectionView，双向滚动]]></title>
    <link href="https://dsencheng.github.io/16145826764719.html"/>
    <updated>2021-03-01T15:11:16+08:00</updated>
    <id>https://dsencheng.github.io/16145826764719.html</id>
    <content type="html"><![CDATA[
<p>注：布局使用了SnapKit</p>

<span id="more"></span><!-- more -->

<p>上图<br/>
<img src="media/16145826764719/Simulator%20Screen%20Shot%20-%20iPhone%20SE%20-2nd%20generation-%20-%202021-03-01%20at%2015.10.26.png" alt="Simulator Screen Shot - iPhone SE -2nd generation- - 2021-03-01 at 15.10.26"/></p>

<p>上代码</p>

<pre><code class="language-text">import UIKit

private let reuseCell = &quot;UICollectionViewCell&quot;
class TestViewController: UIViewController,UICollectionViewDataSource,UICollectionViewDelegate {
    
    override func viewDidLoad() {
        super.viewDidLoad()
        
        let scrollView = UIScrollView()
        scrollView.backgroundColor = .lightGray
        view.addSubview(scrollView)
        scrollView.snp.makeConstraints { (make) in
            make.edges.equalToSuperview()
        }
        
        let containerView = UIView()
        containerView.backgroundColor = .gray
        scrollView.addSubview(containerView)
        containerView.snp.makeConstraints { (make) in
            make.edges.equalToSuperview()
            make.height.width.equalToSuperview()
        }
        
        let layout = UICollectionViewFlowLayout()
        layout.estimatedItemSize = UICollectionViewFlowLayout.automaticSize
        layout.scrollDirection = .horizontal
        
        let collection = UICollectionView(frame: .zero, collectionViewLayout: layout)
        collection.backgroundColor = .white
        collection.register(UICollectionViewCell.self, forCellWithReuseIdentifier: reuseCell)
        collection.delegate = self
        collection.dataSource = self
        containerView.addSubview(collection)
        collection.snp.makeConstraints { (make) in
            make.top.equalTo(100)
            make.left.equalToSuperview()
            make.width.equalToSuperview()
            make.height.equalTo(200)
        }
    }
    
    
    func numberOfSections(in collectionView: UICollectionView) -&gt; Int {
        60
    }
    func collectionView(_ collectionView: UICollectionView, numberOfItemsInSection section: Int) -&gt; Int {
        return 2
    }
    
    func collectionView(_ collectionView: UICollectionView, cellForItemAt indexPath: IndexPath) -&gt; UICollectionViewCell {
        let cell = collectionView.dequeueReusableCell(withReuseIdentifier: reuseCell, for: indexPath)
        let color = CGFloat.random(in: 0...1.0)
        cell.contentView.backgroundColor = UIColor(red: color, green: color, blue: color, alpha: 1)
        return cell
    }

    func collectionView(_ collectionView: UICollectionView, didSelectItemAt indexPath: IndexPath) {
        print(indexPath)
    }
}
</code></pre>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Git命令拾遗]]></title>
    <link href="https://dsencheng.github.io/16136110176093.html"/>
    <updated>2021-02-18T09:16:57+08:00</updated>
    <id>https://dsencheng.github.io/16136110176093.html</id>
    <content type="html"><![CDATA[
<h5 id="toc_0">全局更改提交人姓名和电子邮件</h5>

<h6 id="toc_1">终端操作</h6>

<pre><code class="language-text">$ git config --global user.name &quot;John Doe&quot;
$ git config --global user.email &quot;john@doe.org&quot;
</code></pre>

<h6 id="toc_2">Sourcetree</h6>

<p>仓库 &gt; 仓库设置 &gt; 高级 ，选中<code>使用全局用户设置</code></p>

<h5 id="toc_3">更改每个存储库的提交者名称和电子邮件</h5>

<h6 id="toc_4">终端操作</h6>

<pre><code class="language-text">$ git config user.name &quot;John Doe&quot;
$ git config user.email &quot;john@doe.org&quot;
</code></pre>

<span id="more"></span><!-- more -->

<p><code>cat .git/config</code> 查看配置</p>

<h6 id="toc_5">Sourcetree</h6>

<p>仓库 &gt; 仓库设置 &gt; 高级 ，取消<code>使用全局用户设置</code>，并输入当前仓库的提交者名称和电子邮件。</p>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[折腾Docker之Jenkins]]></title>
    <link href="https://dsencheng.github.io/16124279585782.html"/>
    <updated>2021-02-04T16:39:18+08:00</updated>
    <id>https://dsencheng.github.io/16124279585782.html</id>
    <content type="html"><![CDATA[
<h4 id="toc_0">安装</h4>

<p>1.拉取镜像<br/>
<code>docker pull jenkins/jenkins</code><br/>
注意: <code>docker search jenkins</code> 显示的<code>jenkins</code>,已经废弃，需要使用<code>jenkins/jenkins</code>。</p>

<p>2.运行容器，首次启动需要几分钟。</p>

<pre><code class="language-text">docker run -d \
--name jenkins \
--restart=always \
--privileged=true \
-v $HOME/jenkins_home:/var/jenkins_home \
-p 8080:8080 \
jenkins/jenkins
</code></pre>

<span id="more"></span><!-- more -->

<p>3.打开浏览器，输入<code>127.0.0.1:8080</code>或者宿主机的IP地址，首次访问，需要<code>解锁 Jenkins</code>。根据提示，打开<code>/var/jenkins_home/secrets/initialAdminPassword</code>文件来查看初始化密码。在创建容器时，已经做了目录映射<code>-v $HOME/jenkins_home:/var/jenkins_home</code>，所以打开宿主机的<code>$HOME/jenkins_home/secrets/initialAdminPassword</code>密码文件查看就可以了。</p>

<p>4.输入初始化密码后，点击<code>继续</code>，在<code>自定义Jenkins</code>页面，选择<code>安装推荐的插件</code>，等待插件安装完成。如果你对Jenkins比较了解，也可以选择手动安装插件。<br/>
ps:安装插件出奇的顺利，以前通过其他方式安装，总是会有插件安装失败。而且页面自动中文，不是英文的。</p>

<p>5.插件安装完成后，<code>创建第一个管理员用户</code>，填写用户名，密码，邮箱，点击<code>保存并完成</code>。</p>

<p>6.<code>实例配置</code>页面不做修改，直接<code>保存并完成</code>，最后点击<code>开始使用Jenkins</code>。好了，到这里就完成了，开始使用吧。</p>

<p><strong>注：docker里的Jenkins是Linux环境，不是Mac环境。</strong></p>

<h5 id="toc_1">更新Jenkins</h5>

<pre><code class="language-text"># 进入容器
&gt; docker exec -it -u root jenkins bash

# 进入jenkins.war目录
&gt; cd /usr/share/jenkins/

# 下载新包，因为当前目录已有正在使用的jenkins.war文件，新的文件命名为jenkins.war.1
&gt; wget http://mirrors.jenkins.io/war/latest/jenkins.war
# 上面的地址较慢，可以在Jenkins的升级提示里复制新的下载链接，比如下面的这个链接。
&gt; wget https://updates.jenkins.io/download/war/2.280/jenkins.war
# 或者手动下载，替换容器内的文件。

# 更改新旧文件名
&gt; ls
jenkins.war  jenkins.war.1  ref
&gt; mv jenkins.war jenkins.war.backup
&gt; ls 
jenkins.war.1  jenkins.war.backup  ref
&gt; mv jenkins.war.1 jenkins.war
&gt; ls
jenkins.war  jenkins.war.backup  ref

# 退出容器
&gt; exit

# 重启容器
&gt; docker restart jenkins
</code></pre>

<h4 id="toc_2">添加节点</h4>

<p>iOS任务需要Mac环境，而Docker里的Jenkins是Linux环境，所以还需要添加宿主机为节点。我的宿主机是Mac mini，这里的套娃逻辑如下:</p>

<pre><code class="language-text">Mac &gt; Docker &gt; Jenkins容器(Linux) &gt; 新建节点 &gt; Mac
</code></pre>

<p>打开Jenkins的<code>系统管理</code> &gt; <code>节点管理</code> &gt; <code>新建节点</code>。<br/>
填写<code>节点名称</code>，这个名称在任务配置中指定节点需要填写。<br/>
<code>远程工作目录</code>，填写项目存放路径，例如<code>/Users/dsen/.jenkins</code>。<br/>
<code>用法</code>，选择<code>只允许绑定到这台机器的job</code>。<br/>
<code>启动方式</code>，选择<code>Launch agents via SSH</code>，<code>主机</code>填写IP，如<code>192.168.1.45</code>，不需要添加前缀。<code>Credentials</code>添加节点的登录账户和密码，也就是这台Mac电脑的登录账户。<code>Host Key Verification Strategy</code>选择<code>Non verifying Verifiycation Strategy</code>。</p>

<p><code>节点属性</code> 勾选 <code>环境变量</code>，新增键值，键输入 <code>PATH</code>, 值输入<code>/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:/Users/dsen/.rvm/bin:/usr/local/bin/pod</code>。如果不知道这个值，可以打开节点的终端，输入<code>echo $PATH</code>获得。</p>

<h4 id="toc_3">新建任务</h4>

<p>新建一个任务，名字随便输入，选择<code>构建一个自由风格的软件项目</code>，点击确定。在项目的配置里，<code>构建</code> &gt; <code>增加构建步骤</code> &gt; <code>执行shell</code>，填写<code>uname -a</code>，点击保存。返回项目页面后，点击<code>立即构建</code>，构建完成后，可以在构建历史查看<code>控制台输出</code>。<br/>
如果是<code>Linux 67c7f669fe0b 4.19.121-linuxkit #1 SMP Tue Dec 1 17:50:32 UTC 2020 x86_64 GNU/Linux</code>，说明当前任务是在Linux系统执行的。<br/>
打开项目的设置，勾选<code>限制项目的运行节点</code>，输入之前添加的节点名称，保存之后，再次构建，查看<code>控制台输出</code>，如果是<code>Darwin localhost 19.6.0 Darwin Kernel Version 19.6.0: Sun Jul  5 00:43:10 PDT 2020; root:xnu-6153.141.1~9/RELEASE_X86_64 x86_64</code>，说明当前任务是在Mac系统执行的。</p>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[折腾Docker之Nginx]]></title>
    <link href="https://dsencheng.github.io/16121070654252.html"/>
    <updated>2021-01-31T23:31:05+08:00</updated>
    <id>https://dsencheng.github.io/16121070654252.html</id>
    <content type="html"><![CDATA[
<p>在创建Gitlab时，加了一个参数<code>--hostname gitlab.interotc.com</code>，而现在用<code>http://192.168.0.X:10080</code>的这种方式。显然这种方式并不方便。<br/>
这时候就需要请出Nginx了。<br/>
执行命令<code>docker pull nginx</code>，拉取最新镜像。<br/>
拉取完成后执行命令<code>docker images</code></p>

<pre><code class="language-text">REPOSITORY         TAG           IMAGE ID       CREATED       SIZE
gitlab/gitlab-ce   13.8.1-ce.0   404c168c204f   5 days ago    2.15GB
nginx              latest        f6d0b4767a6c   2 weeks ago   133MB
</code></pre>

<p>可以看到宿主机有两个镜像。<br/>
执行命令<code>docker ps</code></p>

<span id="more"></span><!-- more -->

<pre><code class="language-text">CONTAINER ID   IMAGE                          COMMAND             CREATED        STATUS                  PORTS                                                                  NAMES
14f86e7bba8f   gitlab/gitlab-ce:13.8.1-ce.0   &quot;/assets/wrapper&quot;   23 hours ago   Up 23 hours (healthy)   0.0.0.0:10022-&gt;22/tcp, 0.0.0.0:10080-&gt;80/tcp, 0.0.0.0:10443-&gt;443/tcp   gitlab
</code></pre>

<p>当前有一个名字为<code>gitlab</code>的容器正在运行。<br/>
尝试创建一个Nginx容器。<br/>
执行命令</p>

<pre><code class="language-text">docker run -d \
-p 80:80 -p 443:443 \
--name nginx \
--restart=always \
--privileged=true \
-v $HOME/nginx/html:/usr/share/nginx/html \ #可以去掉
-v $HOME/nginx/conf/nginx.conf:/etc/nginx/nginx.conf \
-v $HOME/nginx/conf/default.conf:/etc/nginx/conf.d/default.conf \
-v $HOME/nginx/logs:/var/log/nginx \
nginx
</code></pre>

<h4 id="toc_0">错误:</h4>

<p><code>not a directory: unknown: Are you trying to mount a directory onto a file (or vice-versa)? Check if the specified host path exists and is the expected type.</code></p>

<p>出现这个问题是<code>-v xxx:xxx</code> 卷映射的问题，<code>default.conf</code> 这种<strong>是文件映射，不是目录</strong>。<br/>
处理方法是先创建一次Nginx容器，把容器内地文件复制出来。然后删除容器，再次重新创建容器，添加卷映射。<br/>
处理步骤</p>

<pre><code class="language-text">1.创建一个默认容器
docker run -p 80:80 --name mynginx -d nginx

2.查看容器ID
docker ps -a
回显
CONTAINER ID   IMAGE  COMMAND                  CREATED          STATUS                PORTS                                                                  NAMES
9fa05ef56aa2   nginx  &quot;/docker-entrypoint.…&quot;   20 minutes ago   Up 20 minutes         0.0.0.0:80-&gt;80/tcp, 0.0.0.0:443-&gt;443/tcp                               otc-nginx

3.复制文件出来，可以使用命令 `mkdir -p ~/nginx/{conf,html,logs}` 提前创建好目录
docker cp 容器ID:/etc/nginx/nginx.conf $HOME/nginx/conf/nginx.conf

4.删除创建的临时容器
docker rm 容器ID

</code></pre>

<p>参考<a href="https://www.cnblogs.com/xishaohui/p/8871994.html">Docker安装nginx以及负载均衡</a></p>

<p>解决错误之后，重新创建容器，完成后打开浏览器，访问宿主机，如<code>http://192.168.0.X</code>。<br/>
此时页面显示如下内容，说明成功。</p>

<pre><code class="language-text">Welcome to nginx!

If you see this page, the nginx web server is successfully installed and working. Further configuration is required.

For online documentation and support please refer to nginx.org.
Commercial support is available at nginx.com.

Thank you for using nginx.
</code></pre>

<p>Nginx已经启动成功，接下来就是添加配置文件，把请求转给gitlab容器。<br/>
假设宿主机的IP是192.168.7.10，宿主机已经启动gitlab和nginx两个docker容器。<br/>
宿主机10080端口映射gitlab的80端口。</p>

<p>1.本机增加host映射</p>

<pre><code class="language-text">127.0.0.1    localhost
::1         localhost
# 新增映射
192.168.7.10 gitlab.interotc.com
</code></pre>

<p>2.增加Nginx配置，重启Nginx容器。</p>

<pre><code class="language-text">server {
    listen       80;
    listen  [::]:80;
    server_name  gitlab.interotc.com;

    location / {
        proxy_set_header Host $proxy_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        # 这里有个坑，nginx容器内的网段和宿主机网段不一样，不要使用localhost或者127.0.0.1。
        # 如果你是安装的nginx，不是采用docker，那么nginx和宿主机是一个网段。
        proxy_pass    http://192.168.7.10:10080;
    }

}

</code></pre>

<p>3.顺利的话，现在访问<code>gitlab.interotc.com</code>就可以显示正确的网页了。</p>

<p>这里有个套娃逻辑。<br/>
本机增加host映射，访问<code>gitlab.interotc.com</code><br/>
⬇️<br/>
发送到<code>192.168.7.10</code>(宿主机)的80端口<br/>
⬇️<br/>
宿主机的80端口已经映射nginx容器的80端口<br/>
请求发送给nginx容器的80端口<br/>
⬇️<br/>
nginx容器收到<code>gitlab.interotc.com</code>请求<br/>
⬇️<br/>
解析配置文件<code>proxy_pass    http://192.168.7.10:10080;</code><br/>
意思是把请求转给<code>192.168.7.10</code>(宿主机)的10080端口<br/>
⬇️<br/>
宿主机的10080端口已经映射gitlab容器的80端口<br/>
请求发给gitlab的80端口<br/>
⬇️<br/>
gitlab的80端口收到请求，返回网页。</p>

<p>在写nginx配置时，需要清楚容器和宿主机的网络关系，在照搬一些文章的nginx配置文件时，<code>proxy_pass    http://localhost:10080;</code>和<code>proxy_pass    http://127.0.0.1:10080;</code>使我走进了一个很大的误区。nginx容器内的<code>localhost</code>是指nginx容器内的Linux网络，并不是指宿主机(我的宿主机是Mac)。</p>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[折腾Docker之Gitlab]]></title>
    <link href="https://dsencheng.github.io/16120859165616.html"/>
    <updated>2021-01-31T17:38:36+08:00</updated>
    <id>https://dsencheng.github.io/16120859165616.html</id>
    <content type="html"><![CDATA[
<h3 id="toc_0">三、安装Gitlab</h3>

<h5 id="toc_1">1、打开终端，搜索gitlab.</h5>

<pre><code class="language-text">&gt; docker search gitlab
</code></pre>

<h5 id="toc_2">2、拉取镜像</h5>

<pre><code class="language-text">&gt; docker pull gitlab/gitlab-ce
</code></pre>

<h5 id="toc_3">3.创建容器</h5>

<p>创建之前，建议打开<code>Docker Desktop</code> - <code>Preferences</code> - <code>Resources</code><br/>
<code>Advanced</code> 页签把 CPU 改成 4， Memory 改成 4GB。<br/>
<code>File sharing</code> 全部删除，然后添加 <code>/Users</code> 或 <code>/Users/用户名</code>，以减少容器访问磁盘。<br/>
终端执行命令：</p>

<pre><code class="language-text">docker run --detach \
--hostname gitlab.interotc.com \
--name gitlab \
--restart always \
--publish 10443:443 --publish 10080:80 --publish 10022:22 \
--volume $HOME/gitlab/config:/etc/gitlab \
--volume $HOME/gitlab/logs:/var/log/gitlab \
--volume $HOME/gitlab/data:/var/opt/gitlab \
--privileged=true \
gitlab/gitlab-ce:13.8.1-ce.0
</code></pre>

<span id="more"></span><!-- more -->

<h6 id="toc_4">命令参数解释</h6>

<p><code>--detach</code> 后台运行<br/>
<code>--hostname gitlab.interotc.com</code> 容器的主机名或域名，即192.168.10.10或者 gitlab.example.com<br/>
<code>--name gitlab</code> 容器的名字<br/>
<code>--restart always</code> 设置重启方式，always 代表一直开启，服务器开机后也会自动开启的<br/>
<code>--publish 10080:80</code>  把本机端口映射到容器端口，即访问宿主机10080端口为容器的80端口<br/>
<code>--volume $HOME/gitlab/config:/etc/gitlab</code> 宿主机目录映射到容器目录<br/>
<code>--privileged=true</code> 提升容器的<code>root</code>用户拥有真正root权限，否则只是普通用户权限<br/>
<code>gitlab/gitlab-ce:13.8.1-ce.0</code> 这里我指定了镜像的tag版本，如果你是最新的版本，可以改为<code>gitlab/gitlab-ce</code></p>

<p>创建成功后会显示一串字符，例如</p>

<pre><code class="language-text">&gt; docker run --detach \                                                                                                                          
--hostname gitlab.interotc.com \
--name gitlab \
--restart always \
--publish 10443:443 --publish 10080:80 --publish 10022:22 \
--volume $HOME/gitlab/config:/etc/gitlab \
--volume $HOME/gitlab/logs:/var/log/gitlab \
--volume $HOME/gitlab/data:/var/opt/gitlab \
--privileged=true \
gitlab/gitlab-ce:13.8.1-ce.0
af8fc3a2499f6ce47d52a64871dd2027bfcb3e17c7ee23fc64026c6f236e09ab
</code></pre>

<p>执行命令<br/>
<code>docker ps</code> 或者 <code>docker container ls</code></p>

<pre><code class="language-text">&gt; docker ps                                                                                                                                      
CONTAINER ID   IMAGE                          COMMAND             CREATED              STATUS                                 PORTS                                                                  NAMES
af8fc3a2499f   gitlab/gitlab-ce:13.8.1-ce.0   &quot;/assets/wrapper&quot;   About a minute ago   Up About a minute (health: starting)   0.0.0.0:10022-&gt;22/tcp, 0.0.0.0:10080-&gt;80/tcp, 0.0.0.0:10443-&gt;443/tcp   gitlab
</code></pre>

<p><code>STATUS</code> 会有下面几种状态 </p>

<pre><code class="language-text">`health: starting`  说明容器正在启动中，还没有启动完成。
`healthy`  说明容器启动完成，一切正常。
`unhealthy`  说明容器启动完成，但是不健康，有问题。建议执行命令重启容器`docker restart gitlab`。
</code></pre>

<p><strong>大坑</strong>:如果状态一直是<code>unhealthy</code>，在更改CPU=4 , Memory=4GB的情况下，状态变为<code>healthy</code>。恢复CPU=2 , Memory=2GB时，状态又变回<code>unhealthy</code>。说明gitlab很消耗性能。<br/>
<strong>深坑</strong>：在启动容器后， com.docker.hyperkit 进程出现高达 100%、200%的CPU使用率，在提高CPU数量，删除<code>File sharing</code>条目之后得到缓解。</p>

<h5 id="toc_5">4、访问gitlab</h5>

<p>本机打开浏览器访问<code>http://127.0.0.1:10080</code>，会显示<code>Change your password</code>，这是第一次访问，修改管理员账户密码。更改完成后，就进入了登录页面，输入<code>root</code>和刚改的密码，就可以进入gitlab的控制台了。<br/>
局域网内其他用户可以通过<code>http://宿主机ip:10080</code>来访问。</p>

<h4 id="toc_6">5.设置语言</h4>

<p>成功登录后，可以设置当前用户的语言为中文。点击右上角的头像，选择<code>Settings</code>，左侧找到<code>Preferences</code>，页面向下滚动就可以看到<code>Localization</code>了，下拉选择中文，最后点击<code>Save changes</code>，刷新页面就可以显示成中文了。如图<br/>
<img src="media/16120859165616/16134863305633.jpg" alt="" style="width:1273px;"/><br/>
<img src="media/16120859165616/16134864292402.jpg" alt="" style="width:1274px;"/></p>

<h4 id="toc_7">6.创建用户</h4>

<p>在使用<code>root</code>用户登录之后，点击导航栏的扳手图标，找到左侧<code>用户</code>，点击右侧按钮<code>新用户</code>，之后填写好必填项，初始密码填写12345678，点击最下面的<code>创建用户</code>按钮就可以了。<br/>
<img src="media/16120859165616/16134870337206.jpg" alt="" style="width:1272px;"/></p>

<h4 id="toc_8">7.创建一个项目</h4>

<p>点击导航栏的<code>Gitlab</code>图标，会显示首页的项目，点击右侧<code>新建项目</code>，选择<code>创建空白项目</code>，也可以选择使用模板创建。<br/>
<img src="media/16120859165616/16134870881325.jpg" alt="" style="width:1267px;"/></p>

<p>项目名称填入<code>demo</code>，点击<code>新建项目</code>按钮完成。</p>

<p><img src="media/16120859165616/16134872391466.jpg" alt="" style="width:1272px;"/></p>

<p>创建完成后，还需要添加SSH，才可以访问仓库。<br/>
<img src="media/16120859165616/16134874595200.jpg" alt="" style="width:1276px;"/></p>

<p>点击<code>添加SSH密钥</code>，在<code>密钥</code>输入框中，填写密钥。如果不知道如何添加，可以点击页面中的<code>生成一个</code>链接，会跳转到帮助页面，有文档教你如何创建密钥。<br/>
下面以Mac系统为例，创建一个<code>ED25519</code>密钥。<br/>
打开命令行，输入<code>ssh-keygen -t ed25519 -C &quot;admin@example.com&quot;</code>回车，记得替换一下邮箱。</p>

<p><img src="media/16120859165616/16134888347946.jpg" alt="" style="width:681px;"/></p>

<p><strong>注意，输入文件名字时，如果没有指定完整路径，文件会存放在当前用户的目录，即<code>/Users/xxx</code></strong></p>

<p>查看一下你的密钥对存放的目录</p>

<pre><code class="language-text">ls ~/.ssh/ #这是默认目录，可以替换指定的存储目录
gitlab_root     gitlab_root.pub
</code></pre>

<p>使用<code>cat</code>命令，查看并复制公钥的内容。复制ssh开头的这行内容。</p>

<pre><code class="language-text">&gt; cat ~/.ssh/gitlab_root.pub                                                                                                                     
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKAxw5whGEq+Itaui0vH0Jic4lb45kg7W7xwVA0f4fy2 admin@example.com
</code></pre>

<p>复制后粘贴到gitlab中的密钥输入框内，点击<code>添加密钥</code>。<br/>
<img src="media/16120859165616/16134894177816.jpg" alt="" style="width:1275px;"/><br/>
<img src="media/16120859165616/16134895554352.jpg" alt="" style="width:1273px;"/></p>

<p>然后回到<code>demo</code>项目的主页，按照<code>命令行指引</code>，这就完成项目的创建了。</p>

<h2 id="toc_9">踩坑</h2>

<p>坑:<br/>
<code>Permission denied (publickey,password,keyboard-interactive).</code></p>

<p><a href="https://stackoverflow.com/questions/1556056/permission-denied-publickey-keyboard-interactive">参考:Permission denied (publickey,keyboard-interactive)</a><br/>
<a href="https://github.com/lqshow/notes/issues/26">参考:SSH Permission denied (publickey,password,keyboard-interactive)</a><br/>
<a href="https://www.cnblogs.com/feiquan/p/13693329.html">参考:Mac下SSH免密登录Permission denied (publickey,password,keyboard-interactive).</a></p>

<p>问题的原因应该是SSH端口的问题。<br/>
gitlab容器创建时，增加了 10022:22 的映射关系，即宿主机 10022 端口映射到容器的 22 端口。在使用 <code>git clone git@gitlab.interotc.com:zzjs/demo.git</code> 命令时会报上面的错误。<br/>
同时又因为本地有多个git环境，不同的账号。<br/>
不断的google之后，处理如下：<br/>
1.创建config文件，来解决不同环境和不同账号的问题。<br/>
2.创建authorized_keys文件，来解决免密登录。</p>

<pre><code class="language-text"># 进入ssh目录
&gt; cd ~/.ssh
# 查看文件，我这里的文件比较多
&gt; ls
gitee_id_rsa      github_id_rsa       gitlab_zzjs         known_hosts
config            gitee_id_rsa.pub    github_id_rsa.pub   gitlab_zzjs.pub


</code></pre>

<p>可以看到已经存在一个 <code>config</code> 文件了，如果没有这个文件，使用 <code>touch config</code> 创建一个就好了。<br/>
在<code>config</code>文件中添加下面的内容</p>

<pre><code class="language-text">Host gitlab.interotc.com
HostName gitlab.interotc.com
User zzjs@example.com
Port 10022
PreferredAuthentications publickey
IdentityFile ~/.ssh/gitlab_zzjs
</code></pre>

<p>当前目录是没有 <code>authorized_keys</code> 文件的，这里使用管道的方式创建文件。</p>

<pre><code class="language-text">&gt;  cat gitlab_zzjs.pub &gt;&gt; authorized_keys
&gt;  chmod 600 authorized_keys
&gt;  cat authorized_keys
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKEY0aS5EkIHAs3PPT2Gv1qjbp14S084qn5t6ryt1O/W zzjs@example.com
</code></pre>

<p>这两步完成后来测试一下</p>

<pre><code class="language-text">&gt; ssh -T git@gitlab.interotc.com
The authenticity of host &#39;[gitlab.interotc.com]:10022 ([x.x.x.x]:10022)&#39; can&#39;t be established.
ECDSA key fingerprint is SHA256:zX6tCKkYn+/77AMxBN0OWr1uoo43jKCJqp+2bGt4PVY.
Are you sure you want to continue connecting (yes/no/[fingerprint])? yes
Warning: Permanently added &#39;[gitlab.interotc.com]:10022&#39; (ECDSA) to the list of known hosts.
Welcome to GitLab, @zzjs!
</code></pre>

<p>再次测试，并<code>git clone</code>，成功！</p>

<pre><code class="language-text">&gt; ssh -T git@gitlab.interotc.com
Welcome to GitLab, @zzjs!
&gt; git clone git@gitlab.interotc.com:zzjs/demo.git
Cloning into &#39;demo&#39;...
warning: You appear to have cloned an empty repository.
</code></pre>

<p>虽然解决了问题，但对于本地来说设置和操作过多，后续尝试使用Nginx去解决这个问题。</p>

<p>坑:<br/>
<code>Pushing to http://gitlab.interotc.com/zzjs/demo.git<br/>
remote: You are not allowed to push code to this project.<br/>
fatal: unable to access &#39;http://gitlab.interotc.com/zzjs/demo.git/&#39;: The requested URL returned error: 403</code></p>

<p>这是使用Sourcetree，在推送时的报错。暂时较忙，也没有启用https，在新建克隆时，把http的请求改成git的请求后，问题不在出现。</p>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[折腾Docker安装]]></title>
    <link href="https://dsencheng.github.io/16118025704485.html"/>
    <updated>2021-01-28T10:56:10+08:00</updated>
    <id>https://dsencheng.github.io/16118025704485.html</id>
    <content type="html"><![CDATA[
<h3 id="toc_0">一. 安装 Docker</h3>

<p>下载 <a href="https://www.docker.com/products/docker-desktop">Docker Desktop</a> 按提示进行安装。</p>

<p>安装完成后，打开<code>Docker Desktop</code>，选择<code>Preferences</code>，然后选择<code>Docker Engine</code>，加入下面国内镜像源，点击<code>Apply &amp; Restart</code>。</p>

<pre><code class="language-text">{
    &quot;registry-mirrors&quot;: [
    &quot;https://docker.mirrors.ustc.edu.cn&quot;,
    &quot;https://hub-mirror.c.163.com&quot;,
    &quot;https://mirror.baidubce.com&quot;,
    &quot;https://reg-mirror.qiniu.com&quot;,
    &quot;https://mirror.ccs.tencentyun.com&quot;
  ]
}
</code></pre>

<h3 id="toc_1">二. 安装 Kubernetes(可选)</h3>

<p><strong>注：如果是简单使用Docker，可跳过此步骤。</strong></p>

<p>因为中国大陆网络限制的原因，直接<code>Enable Kubernetes</code>，会一直卡在<code>starting</code>的状态。</p>

<span id="more"></span><!-- more -->

<h5 id="toc_2">深坑：Kubernetes is starting</h5>

<p><strong>参考：<a href="https://github.com/AliyunContainerService/k8s-for-docker-desktop">k8s-for-docker-desktop</a> 解决方案。</strong></p>

<p>1、首先克隆仓库到本地。 <br/>
2、终端打开<code>k8s-for-docker-desktop</code>目录，执行<code>./load_images.sh</code> 命令，等待完成。<br/>
3、打开<code>Docker Desktop</code>，选择<code>Preferences</code>，打开左侧<code>Kubernetes</code>,勾选<code>Enable Kubernetes</code>,点击<code>Apply &amp; Restart</code>。等待完成。如果有弹窗，点击<code>Install</code>。</p>

<h3 id="toc_3">一. 初步使用 Docker</h3>

<p>1.搜索镜像<br/>
<code>docker search 关键字</code><br/>
示例</p>

<pre><code class="language-text">&gt; docker search hello
NAME                                       DESCRIPTION                                     STARS     OFFICIAL   AUTOMATED
hello-world                                Hello World! (an example of minimal Dockeriz…   1379      [OK]
kitematic/hello-world-nginx                A light-weight nginx container that demonstr…   148
tutum/hello-world                          Image to test docker deployments. Has Apache…   78                   [OK]
nginxdemos/hello                           NGINX webserver that serves a simple page co…   65                   [OK]
</code></pre>

<p>2.拉取镜像<br/>
<code>docker pull 关键字</code><br/>
示例</p>

<pre><code class="language-text">&gt; docker pull hello-world
Using default tag: latest
latest: Pulling from library/hello-world
0e03bdcc26d7: Pull complete
Digest: sha256:31b9c7d48790f0d8c50ab433d9c3b7e17666d6993084c002c2ff1ca09b96391d
Status: Downloaded newer image for hello-world:latest
docker.io/library/hello-world:latest
</code></pre>

<p>3.查看镜像<br/>
<code>docker images</code> 或 <code>docker image ls</code><br/>
示例</p>

<pre><code class="language-text">&gt; docker images
REPOSITORY         TAG           IMAGE ID       CREATED         SIZE
jenkins/jenkins    latest        0d3e7552f52a   7 days ago      721MB
gitlab/gitlab-ce   13.8.1-ce.0   404c168c204f   2 weeks ago     2.15GB
nginx              latest        f6d0b4767a6c   4 weeks ago     133MB
hello-world        latest        bf756fb1ae65   13 months ago   13.3kB
</code></pre>

<p>4.删除镜像<br/>
<code>docker image rm 镜像ID或镜像名</code> 或 <code>docker rmi 镜像ID或镜像名</code><br/>
示例</p>

<pre><code class="language-text"># 删除镜像
&gt; docker image rm hello-world
Untagged: hello-world:latest
Untagged: hello-world@sha256:31b9c7d48790f0d8c50ab433d9c3b7e17666d6993084c002c2ff1ca09b96391d
Deleted: sha256:bf756fb1ae65adf866bd8c456593cd24beb6a0a061dedf42b26a993176745f6b
Deleted: sha256:9c27e219663c25e0f28493790cc0b88bc973ba3b1686355f221c38a36978ac63
# 查看镜像
&gt; docker images
REPOSITORY         TAG           IMAGE ID       CREATED       SIZE
jenkins/jenkins    latest        0d3e7552f52a   7 days ago    721MB
gitlab/gitlab-ce   13.8.1-ce.0   404c168c204f   2 weeks ago   2.15GB
nginx              latest        f6d0b4767a6c   4 weeks ago   133MB
# 拉取镜像
&gt; docker pull hello-world
Using default tag: latest
latest: Pulling from library/hello-world
0e03bdcc26d7: Pull complete
Digest: sha256:31b9c7d48790f0d8c50ab433d9c3b7e17666d6993084c002c2ff1ca09b96391d
Status: Downloaded newer image for hello-world:latest
docker.io/library/hello-world:latest
# 查看镜像
&gt; docker images
REPOSITORY         TAG           IMAGE ID       CREATED         SIZE
jenkins/jenkins    latest        0d3e7552f52a   7 days ago      721MB
gitlab/gitlab-ce   13.8.1-ce.0   404c168c204f   2 weeks ago     2.15GB
nginx              latest        f6d0b4767a6c   4 weeks ago     133MB
hello-world        latest        bf756fb1ae65   13 months ago   13.3kB
# 删除镜像
&gt; docker rmi bf756fb1ae65
Untagged: hello-world:latest
Untagged: hello-world@sha256:31b9c7d48790f0d8c50ab433d9c3b7e17666d6993084c002c2ff1ca09b96391d
Deleted: sha256:bf756fb1ae65adf866bd8c456593cd24beb6a0a061dedf42b26a993176745f6b
Deleted: sha256:9c27e219663c25e0f28493790cc0b88bc973ba3b1686355f221c38a36978ac63
# 查看镜像
&gt; docker images
REPOSITORY         TAG           IMAGE ID       CREATED       SIZE
jenkins/jenkins    latest        0d3e7552f52a   7 days ago    721MB
gitlab/gitlab-ce   13.8.1-ce.0   404c168c204f   2 weeks ago   2.15GB
nginx              latest        f6d0b4767a6c   4 weeks ago   133MB

</code></pre>

<p>5.删除重复的镜像<br/>
<code>docker rmi 镜像名:TAG</code></p>

<p>6.删除容器,删除之前需要停止容器，否则会报错</p>

<pre><code class="language-text">&gt; docker rm gitlab                                                                                                                               
Error response from daemon: You cannot remove a running container 381f7be56483d7ae3ee2362c3c7e4d041d6dc5e08c368de65807f383c253c3f2. Stop the container before attempting removal or force remove

&gt; docker stop gitlab  
gitlab

</code></pre>

<p>7.更新镜像<br/>
先拉取最新镜像，完成后查看镜像，其中一个<code>TAG</code>为<code>&lt;none&gt;</code>，把这个镜像删除即可。<br/>
如果有容器正在使用旧的镜像，删除是会报错的。需要先停止并删除容器，在删除镜像，使用新的镜像创建新的容器。</p>

<p><strong>踩坑：</strong></p>

<pre><code class="language-text"> &gt; docker search gitlab
 Error response from daemon: Get https://index.docker.io/v1/search?q=gitlab&amp;n=25: net/http: TLS handshake timeout
</code></pre>

<blockquote>
<p>如果已经配置<code>registry-mirrors</code>为国内镜像，仍然出现上面报错。尝试如下步骤:<strong>1.重启Docker 2.关闭终端 3.重新打开终端</strong>。</p>
</blockquote>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[VSCode设置代理]]></title>
    <link href="https://dsencheng.github.io/16061203734084.html"/>
    <updated>2020-11-23T16:32:53+08:00</updated>
    <id>https://dsencheng.github.io/16061203734084.html</id>
    <content type="html"><![CDATA[
<p>如果出现插件的安装，和包的下载，长时间处于loading的状态，说明某些服务已经被墙，无法访问。这是就需要添加代理，来正常使用。</p>

<p>本机已有<code>v2ray</code>代理服务，或其他代理、翻墙服务。</p>

<p>打开VSCode的<code>设置</code>，输入<code>代理</code>，找到<code>Http:Proxy</code>项，输入代理地址，例如<code>http://127.0.0.1:1087</code>。</p>

<p>这里遇见一个坑，在设置完成后。比如安装好Flutter插件，很多服务依然无法访问。</p>

<p>这时需要对<code>Http:Support</code>项做修改，默认应该是<code>override</code>，修改成<code>on</code>，退出VSCode，重启后插件的代理就生效了。</p>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[文章收藏]]></title>
    <link href="https://dsencheng.github.io/16004385011746.html"/>
    <updated>2020-09-18T22:15:01+08:00</updated>
    <id>https://dsencheng.github.io/16004385011746.html</id>
    <content type="html"><![CDATA[
<p><a href="https://juejin.im/post/6860260103791050760#heading-29">iOS 的自动构建流程</a></p>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[使用Python写iOS Word文档]]></title>
    <link href="https://dsencheng.github.io/15982325565462.html"/>
    <updated>2020-08-24T09:29:16+08:00</updated>
    <id>https://dsencheng.github.io/15982325565462.html</id>
    <content type="html"><![CDATA[
<p>背景：公司要求把项目中的每一个方法都编写入技术文档里。基本格式如下：</p>

<pre><code class="language-text">方法功能描述

方法定义
类名:
UIView
方法名:
layoutSubviews
方法入参:
无
抛出异常:
无
方法返回值:
void
方法逻辑

</code></pre>

<p>同事从网上找了一份Python代码，代码分析错误比较多，自己改了一份，可以应付大部分情况。<br/>
<strong>llvm或Xcode的语法分析没找到，还望知道的读者不吝赐教。</strong><br/>
<span id="more"></span><!-- more --></p>

<p>iOS开发语言分<code>objective-c</code>和<code>swift</code>，所以语法分析也要写两份。<br/>
首先从<code>objective-c</code>开始。</p>

<p>OC语言的方法声明:</p>

<pre><code class="language-text">- (instancetype)initWithFrame:(CGRect)frame
</code></pre>

<ul>
<li>方法由 <code>+</code> 或 <code>-</code> 两个符号开头，表示类方法和实例方法。</li>
<li><code>(instancetype)</code>表示返回值类型，没有返回值表示为<code>(void)</code>。</li>
<li><code>initWithFrame:</code>是方法名字。</li>
<li><code>(CGRect)frame</code>是方法需要的参数。</li>
</ul>

<p>由于需要统计所有的方法，因为<code>.h</code>文件方法不全，所以直接从<code>.m</code>文件分析。</p>

<h5 id="toc_0">1、解析出<code>.m</code>文件中的所有方法名</h5>

<p><code>.m</code>文件里的方法无论分几行，开头是由<code>+(</code> 或 <code>-(</code> ，结尾是 <code>{</code> ，也就是方法实现的第一个大括号，这样解析方法名的逻辑就锁定了。<strong><em>此处极小概率会把运算符号解析。</em></strong></p>

<pre><code class="language-text">def getOCFileMethdLineCountArray(iOSUrlFilePath):
    arr = []
    iOSUrlFile_object = open(iOSUrlFilePath, &#39;r&#39;)
    methodName = &#39;&#39;
  
    try: 
        for lineIdx,line in enumerate(iOSUrlFile_object):

            # 仅判断符号条件的方法
            if line.startswith(&#39;+(&#39;) or line.startswith(&#39;-(&#39;) or line.startswith(&#39;+ (&#39;) or line.startswith(&#39;- (&#39;) :
                methodName += line
                # 匹配到方法的大括号
                if &#39;{&#39; in methodName:
                    arr.append((os.path.basename(iOSUrlFilePath).split(&#39;.&#39;)[0] ,methodName))
                    methodName = &#39;&#39;

                    
    finally:
        iOSUrlFile_object.close()
    return arr
</code></pre>

<h5 id="toc_1">2、解析出完整方法中的各项说明</h5>

<p>按照文档要求，需要解析出<code>方法名</code>，<code>返回值</code>，<code>方法参数</code>3个内容。<br/>
这里罗列了一些常见的方法实现。</p>

<pre><code class="language-text">-(NSString *)bankCard:(Int)card apiKey:(NSString *)apiKey secretKey:(NSString *)secretKey cardImageUrl:(NSString * _Nonnull)imageUrl block:(cardOCRBlock _Nullable)completion {
-(NSString *)stringUrlEncodeing:(NSString *)str {
-(id)init{
-(void)cancelRequest {
- (id)initWithFrame:(CGRect)frame{
- (void)updateWithConfig: (void(^)(Config *config))block  {
- (NSMutableArray&lt;UIButton *&gt; *)itemBtns{
- (void)layoutSubviews {
- (void(^)(TestModel * ))initWithBlock:(void(^)(NSString*))block{
</code></pre>

<p>如果从头部解析，复杂度比较高。从尾部开始反而会比较简单，像贪吃蛇一样，一点一点从尾部吃掉内容。</p>

<pre><code class="language-text">def anyliseOC(text):
    
    methodName = &#39;&#39;
    params = []
    returnValue = &#39;&#39;

    # 去掉关键字
    repArray = [&#39;_Nonnull&#39;,&#39;_Nullable&#39;,&#39;__nullable&#39;,&#39;__nonnull&#39;,&#39;+&#39;,&#39;-&#39;]
    ss1 = text.replace(&#39;{&#39;,&#39;&#39;)
    for rep in repArray:
        ss1 = ss1.replace(rep,&#39;&#39;)
    # 反向解析参数
    rfIndex = ss1.rfind(&#39;:&#39;)
    if rfIndex != -1:
        while rfIndex != -1:
            # 最后的参数部分
            pas = ss1[rfIndex+1:].strip()
            # 去掉参数类型的左右两端小括号
            pas = pas.replace(&#39;(&#39;,&#39;&#39;,1)
            rIdx = pas.rfind(&#39;)&#39;)
            if rIdx != -1:
                pas = pas[:rIdx] + pas[rIdx+1:]
            # 多个空格合并成一个空格,执行2次空格替换。
            pas = pas.replace(&#39;  &#39;,&#39; &#39;).replace(&#39;  &#39;,&#39; &#39;).strip()
            # 添加参数数组中
            params.insert(0,pas)

            # 截掉解析过的尾部
            ss1 = ss1[:rfIndex+1].strip()
            # 最后方法名。向前解析，是否遇到空格或右括号
            spIndex = ss1.rfind(&#39; &#39;,0,rfIndex)
            spIndex1 = ss1.rfind(&#39;)&#39;,0, rfIndex)
            # 先找到有括号
            if spIndex1 &gt; spIndex:
                spIndex = spIndex1
            # 解析到方法名
            if spIndex != -1:
                fc = ss1[spIndex+1:rfIndex+1]
                methodName = fc + methodName 

            # 截掉解析过的尾部
            ss1 = ss1[:spIndex+1]

            # 开始下一次解析
            rfIndex = ss1.rfind(&#39;:&#39;)
            
        
    else:
        # 没有参数的方法
        # 方法名。向前解析，是否遇到空格或右括号
            ss1 = ss1.strip()
            spIndex = ss1.rfind(&#39; &#39;)
            spIndex1 = ss1.rfind(&#39;)&#39;)
            # 先找到有括号
            if spIndex1 &gt; spIndex:
                spIndex = spIndex1
            # 解析到方法名
            if spIndex != -1:
                fc = ss1[spIndex+1:]
                methodName = fc + methodName 
            
            # 截掉解析过的尾部
            ss1 = ss1[:spIndex]
            
    # 返回值处理两端的括号，最后的* 
    returnValue = ss1.strip()
    if returnValue.startswith(&#39;(&#39;):
        returnValue = returnValue[1:].strip()
    if returnValue.endswith(&#39;)&#39;):
        returnValue = returnValue[:-1].strip()
    if returnValue.endswith(&#39;*&#39;):
        returnValue = returnValue[:-1].strip()
    print(&#39;方法名：{}&#39;.format(methodName))
    print(&#39;参数：{}&#39;.format(params))
    print(&#39;返回值：{}&#39;.format(returnValue))

</code></pre>

<h5 id="toc_2">4、解析swift文件中的所有方法。</h5>

<p><code>swift</code>语法中，<code>func</code>为关键字。直接搬运同事的代码，稍作修改。这里采用从头到尾读取解析。像<code>init</code> ，<code>deinit</code>这类没有<code>func</code>关键字的方法无法准确识别，略有瑕疵。</p>

<pre><code class="language-text">def getSwiftFileMethdLineCountArray(iOSUrlFilePath):
    arr = []

    iOSUrlFile_object = open(iOSUrlFilePath, &#39;r&#39;)
    # methodName = &#39;&#39;
    
    fc1 = re.compile(r&#39;[,:=]+&#39;)
    try: 
        lines = enumerate(iOSUrlFile_object)
        classLine = &#39;&#39;
        startClass = False
        funcLine = &#39;&#39;
        startFunc = False
        for lineIdx,line in lines:
            # 去除空格,换行符,制表符
            # line = &quot;&quot;.join(line.split())
            #判断是否是空行或注释行
            if not len(line)  or line.replace(&#39; &#39;,&#39;&#39;).startswith(&#39;/*&#39;) or line.replace(&#39; &#39;,&#39;&#39;).startswith(&#39; *&#39;) or line.replace(&#39; &#39;,&#39;&#39;).startswith(&#39;&gt;&gt;&gt;&#39;) or line.replace(&#39; &#39;,&#39;&#39;).startswith(&#39;//&#39;) or line.replace(&#39; &#39;,&#39;&#39;).startswith(&#39;*/&#39;) or line.replace(&#39; &#39;,&#39;&#39;).startswith(&#39;///&#39;):  
                continue
            else:
                line = line.replace(&#39;  &#39;,&#39;&#39;)
                
                if  line.startswith(&#39;init&#39;) or \
                    line.startswith(&#39;deinit&#39;) or \
                    &quot; init&quot; in line or \
                    &quot;init &quot; in line or \
                    &quot;func &quot; in line or \
                    &quot; func&quot; in line or \
                    &quot;func\n&quot; in line or \
                    startFunc :
                    # 如果init前面含有符号,这应该是使用，而不是声明。
                    indexf = line.find(&#39; init&#39;)
                    if indexf &lt; 0:
                        indexf = line.find(&#39;init &#39;)
                    if indexf &gt;= 0 and re.search(fc1,line[:indexf]) is not None :
                        continue

                    funcLine += line.replace(&#39;\n&#39;,&#39; &#39;).replace(&#39;  &#39;,&#39;&#39;)
                    if line.find(&#39;{&#39;) &lt; 0:
                        startFunc = True
                        continue
                    else:
                        startFunc = False
                        arr.append((os.path.basename(iOSUrlFilePath).split(&#39;.&#39;)[0] ,funcLine))
                        funcLine = &#39;&#39;
    finally:
        iOSUrlFile_object.close()
    return arr

</code></pre>

<h5 id="toc_3">5、解析<code>swift</code>方法中的各项内容</h5>

<p><code>swift</code>方法使用<code>func</code>关键字，方法说明和各项参数都是放在<code>()</code>之内，返回值是在 <code>-&gt;</code>到 <code>{</code> 符号之间，所以这里从头尾到中间的方式读取解析。</p>

<blockquote>
<p><strong>这里的方法名分析比较简易，没有去掉参数，有需要的读者还需进一步补充逻辑。</strong></p>
</blockquote>

<pre><code class="language-text">def anyliseSwift(oriText):
    
    # 去除方法名中的一些关键字
    oriText = oriText.replace(&#39;@escaping&#39;,&#39;&#39;).replace(&#39;public&#39;,&#39;&#39;)\
        .replace(&#39;override&#39;,&#39;&#39;).replace(&#39;required&#39;,&#39;&#39;).replace(&#39;open&#39;,&#39;&#39;)\
            .replace(&#39;private&#39;,&#39;&#39;).replace(&#39;convenience&#39;,&#39;&#39;).strip()
    # 获取方法完整名。
    funcName = u&#39;&#39;
    beganIndex = oriText.find(&#39;func &#39;)
    endIndex = oriText.find(&#39;{&#39;)
    if beganIndex != -1 :
        # 加上func字符串长度
        funcName = oriText[beganIndex + 5:endIndex].strip()
    else:
        # 没有func开头的方法名不在额外处理
        funcName = oriText[:endIndex].strip()

    # 获取返回值
    retIndex = oriText.rfind(&#39;-&gt;&#39;)
    retValue = u&quot;无&quot;
    if retIndex != -1:
        retValue = oriText[retIndex+2:endIndex].strip()
    # 这里会因为闭包解析出错，小括号会被拆分,造成左右不等量，处理一下。
    if retValue.count(&#39;(&#39;) != retValue.count(&#39;)&#39;):
        retValue = u&quot;无&quot;
        retIndex = endIndex

    # 获取参数
    pStart = oriText.find(&#39;(&#39;)
    pEnd = oriText.rfind(&#39;)&#39;,0, retIndex)
    fullParam = oriText[pStart+1:pEnd].strip()
    params = fullParam.split(&#39;,&#39;)
    if len(params[0]) == 0:
        params[0] = u&#39;无&#39;
    else:
        #格式化每个参数
        for i,p in enumerate(params):
            # 去掉开头的下划线，去掉第一个分号，双空格替换成一个。
            if p.startswith(&#39;_&#39;):
                p = p[1:]
            p = p.strip().replace(&#39;:&#39;,&#39; &#39;,1).replace(&#39;  &#39;,&#39; &#39;)
            # 去掉方法中的参数说明。
            if len(p.split(&#39; &#39;)) == 3:
                idx = p.find(&#39; &#39;)
                p = p[idx:].strip()
            params[i] = p
    
    return (funcName,params,retValue)

</code></pre>

<h5 id="toc_4">6、生成Word文档。</h5>

<pre><code class="language-text"># 方法的固定功能说明。
def generateSumary(oriText,className):
    if oriText.find(&#39;init&#39;) &gt;= 0:
        return u&quot;类初始化。&quot;
    elif oriText.lower().find(&#39;viewdidload&#39;) &gt;= 0:
        return u&quot;页面加载完成。&quot;
    elif oriText.lower().find(&#39;viewdidappear&#39;) &gt;= 0:
        return &quot;页面已经显示。&quot;
    elif oriText.lower().find(&#39;viewwillappear&#39;) &gt;= 0:
        return &quot;页面将要显示。&quot;
    elif oriText.lower().find(&#39;viewwilldisappear&#39;) &gt;= 0:
        return u&quot;页面将要消失。&quot;
    elif oriText.lower().find(&#39;viewdiddisappear&#39;) &gt;= 0:
        return u&quot;页面已经消失。&quot;
    else:
        return u&quot;&quot;

# 设置段落格式
def setText(doc,text,size,bold):
    p1 = doc.add_paragraph()
    p1.paragraph_format.first_line_indent = Cm(0.74)  
    p1.paragraph_format.line_spacing = 1.5 # 1.5倍行距 
    l1 = p1.add_run(text)
    l1.font.size = size
    l1.font.name = u&quot;宋体&quot;
    l1.bold = bold
        

# 具体内容添加
def addDetail(doc,text1,textArr):
    setText(doc,text1,Pt(14),True)
    for text in textArr:
        # print(text+&#39;\n&#39;)
        setText(doc,text,Pt(14),False)

# 将源文件分析完成后，输出到目标目录下
def writeToWord(sourcePath,outPath):
    # 创建空文档
    document = Document()
    document.styles[&#39;Normal&#39;].font.name = u&#39;宋体&#39;
    document.styles[&#39;Normal&#39;]._element.rPr.rFonts.set(qn(&#39;w:eastAsia&#39;), u&#39;宋体&#39;)
    # 添加标题，设置级别level，0为Title，1或省略为Heading 1，0&lt;=level&lt;=9
    # document.add_heading(&#39;Document Title&#39;, 0)

    path = sourcePath+&#39;&#39;
    arrT = []

    for dirpath, dirname, filenames in os.walk(path):   
        for filename in filenames:
            if &#39;.m&#39; in filename or &#39;.mm&#39; in filename:
                filePath = os.path.join(dirpath, filename)
                MacroWithCountDicts = getOCFileMethdLineCountArray(filePath)
                print(&#39;=====================&#39;)
                print(&#39;{} 有 {} 个方法&#39;.format(filename,len(MacroWithCountDicts)))
                arrT += MacroWithCountDicts

            elif &#39;.swift&#39; in filename:
                filePath = os.path.join(dirpath, filename)
                MacroWithCountDicts = getSwiftFileMethdLineCountArray(filePath)
                print(&#39;=====================&#39;)
                print(&#39;{} 有 {} 个方法&#39;.format(filename,len(MacroWithCountDicts)))
                arrT += MacroWithCountDicts


    print(&quot;********************************&quot;)
    print(&quot;一共有 {} 个方法&quot;.format(len(arrT)))
    print(&quot;********************************&quot;)
    for result in arrT:
        fullFuncName = result[1]
        allMsg = []
        if fullFuncName.strip().startswith(&#39;+&#39;) or fullFuncName.strip().startswith(&#39;-&#39;):
            allMsg = anyliseOC(fullFuncName)
        else:
            allMsg = anyliseSwift(fullFuncName)

        className = result[0]
        funcName = allMsg[0]
        paras = allMsg[1]
        returnStr = allMsg[2]
        # # 函数名设置为4级标题
        document.add_heading(funcName,level=4)

        # #功能设置为5级标题
        document.add_heading(u&quot;方法功能描述&quot;,level=5)

        setText(document,generateSumary(funcName,className),Pt(14),False)

        # 方法定义设置为5级标题
        document.add_heading(u&quot;方法定义&quot;,level=5)
        addDetail(document,u&quot;类名:&quot;,[className])
        addDetail(document,u&quot;方法名:&quot;,[funcName])
        addDetail(document,u&quot;方法入参:&quot;,paras)
        addDetail(document,u&quot;抛出异常:&quot;,[u&quot;无&quot;])
        addDetail(document,u&quot;方法返回值:&quot;,[returnStr])

        # 方法的逻辑设置为5级标题
        document.add_heading(u&quot;方法逻辑&quot;,level=5)
        # 一些固定方法填充内容。
        # if &#39;NSCoder&#39; in funcName:
        #     setText(document,u&quot;调用fatalError方法。&quot;,Pt(14),False)
        # elif &#39;init&#39; in funcName:
        #     setText(document,u&quot;父类初始化，&quot;,Pt(14),False)
        # else:
        setText(document,u&quot;&quot;,Pt(14),False)

    try:
        document.save(outPath+&quot;生成iOS方法文档.docx&quot;)
    except Exception:
        print(u&quot;检查下当前目录是否存在,是否包含/&quot;)
    else:
        print(u&quot;完成,请打开&quot;+outPath+u&quot;目录下的文件&quot;)
        
  
</code></pre>

<p>虽然这份Python代码还有诸多缺陷，但已达到预计实现的90%以上，没有在继续完善。如果没有达到你的要求，自己动手补充一下吧！欢迎反馈补充。</p>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Jenkins 配置]]></title>
    <link href="https://dsencheng.github.io/15960887152430.html"/>
    <updated>2020-07-30T13:58:35+08:00</updated>
    <id>https://dsencheng.github.io/15960887152430.html</id>
    <content type="html"><![CDATA[
<h4 id="toc_0">当前环境为Mac OS</h4>

<h4 id="toc_1">在 <code>Build with Parameters</code> 时显示当前的pod版本号，并可以在打包时修改。效果如下：</h4>

<p><img src="media/15960887152430/15960917465536.jpg" alt="" style="width:421px;"/></p>

<span id="more"></span><!-- more -->

<p>首先安装 <a href="https://plugins.jenkins.io/uno-choice/">Active Choices Plugins</a>，安装完成后打开项目 -&gt; 配置 -&gt; 勾选参数化构建过程 -&gt; 添加参数 -&gt; Active Choices Parameter, 填写 <code>Name</code>，勾选 <code>Groovy Script</code>, 在<code>Script</code>填写脚本</p>

<pre><code class="language-text">//这里使用项目的绝对路径(absolute path)
def shell = [&#39;/bin/bash&#39;, &#39;-c&#39;, &quot;grep -E &#39;s.version.*=&#39; /Users/你的用户名(YourName)/.jenkins/workspace/iOS-SDK/Demo/ITOPOD/ITOSDK.podspec&quot;]

def process = shell.execute()
List&lt;String&gt; versions = new ArrayList&lt;String&gt;()
versions.add(process.text + &#39;:selected&#39;)

return versions
</code></pre>

<p>在 <code>Fallback Script</code> 填写 </p>

<pre><code class="language-text">return [&#39;error&#39;]
</code></pre>

<p><code>Choice Type</code> 选择 <code>Radio Buttons</code></p>

<p><img src="media/15960887152430/15960900655226.jpg" alt=""/></p>

<p>现在已经完成了显示版本号，接下来比较简单，添加一个字符参数即可。如图：<br/>
<img src="media/15960887152430/15960903221998.jpg" alt="" style="width:736px;"/></p>

<p>点击 <code>保存</code> 后就完成了！</p>

<h4 id="toc_2">构建Shell</h4>

<p>这里补充自己写的构建shell脚本，读者可以参考。</p>

<pre><code class="language-text">export LANG=en_US.UTF-8

#如果填写了PodSpec版本号，修改版本值
if [ $PodSpecVersion ];then
echo &quot;输入版本号:$PodSpecVersion&quot;
sed -i &#39;&#39; -e &quot;s/s.version .*$/s.version          = \&#39;${PodSpecVersion}\&#39;/1&quot; Demo/ITOPOD/ITOSDK.podspec
fi
# 获取podspec版本
VersionNumber=`grep -E &#39;s.version.*=&#39; Demo/ITOPOD/ITOSDK.podspec | tr -d &quot;&#39;a-z= &quot; | sed &quot;s/\.//1&quot;`

echo ${VersionNumber}

if [ ! $VersionNumber ];then
echo &quot;获取版本号错误&quot;
exit -1
fi

# === 打包 === #

# 进入目录
cd ITOSDK_Proj
# 安装依赖库
pod install
# 运行单元测试
#xcodebuild test -workspace &#39;ITOSDK_Proj.xcworkspace&#39; -scheme &#39;ITOSDK_Proj&#39; -destination &#39;platform=iOS Simulator,name=iPhone 8&#39; -only-testing:&#39;ITOSDKTests&#39;

# 执行打包脚本
xcodebuild -workspace &#39;ITOSDK_Proj.xcworkspace&#39; -scheme Pack_Full
# 返回上级目录
cd ..
# 删除并替换文件
rm -rf Demo/ITOPOD/ITOSDK/* 
mv -f ITOSDK_Proj/Z_PackedFramework/ITOSDK.framework Demo/ITOPOD/ITOSDK/ITOSDK.framework

# 进入demo项目文件夹
cd Demo/ITOSDKDemo/
# 安装依赖库
pod install
# demo项目build
xcodebuild -workspace &#39;ITOSDKDemo.xcworkspace&#39; -scheme &#39;ITOSDKDemo&#39;

# 返回项目目录
cd ../..
# zip压缩包Demo文件夹
ZipName=&#39;ITOSDK&amp;DEMO-带双录-V&#39;${VersionNumber}
zip -r -q ${ZipName}.zip Demo
# 移动文件到指定文件目录
rm -rf ~/Desktop/iOSSDK/*
mv ${ZipName}.zip ~/Desktop/iOSSDK/
#列出文件
ls ~/Desktop/iOSSDK


# 上传SVN
#svn update
#svn commit -m “提交sdk和demo压缩包“ ${ZipName}.zip Demo
# 删除本地文件

# 通知相关人员
</code></pre>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[【转载】树莓派配置v2ray客户端]]></title>
    <link href="https://dsencheng.github.io/15936838298753.html"/>
    <updated>2020-07-02T17:57:09+08:00</updated>
    <id>https://dsencheng.github.io/15936838298753.html</id>
    <content type="html"><![CDATA[
<p>点击打开<a href="https://www.ichenfei.com/2019/11/24/%E6%A0%91%E8%8E%93%E6%B4%BE%E9%85%8D%E7%BD%AE%E4%BD%BF%E7%94%A8v2ray%E5%AE%A2%E6%88%B7%E7%AB%AF/">原文链接</a>，本文有修改。</p>

<h2 id="toc_0">下载安装</h2>

<p>使用一键安装脚本安装，参考v2ray官方教程-下载安装</p>

<pre><code class="language-text">bash &lt;(curl -L -s https://install.direct/go.sh)
</code></pre>

<p>可能会出现网络错误<code>Failed to fetch release information. Please check your network or try again.</code></p>

<span id="more"></span><!-- more -->

<p>这就需要从github下载安装包, 先从<a href="https://github.com/v2ray/v2ray-core/releases">v2ray-core/releases</a>将安装包v2ray-linux-arm.zip下载到树莓派, 使用--local参数从本地安装</p>

<pre><code class="language-text">wget https://github.com/v2ray/v2ray-core/releases/download/v4.25.1/v2ray-linux-arm.zip
wget -O v2ray_install.sh  https://install.direct/go.sh
chmod +x v2ray_install.sh
sudo ./v2ray_install.sh --local v2ray-linux-arm.zip

</code></pre>

<h2 id="toc_1">配置v2ray客户端</h2>

<blockquote>
<p><strong>建议</strong>:如果已经有<a href="https://github.com/yanue/V2rayU">V2rayU</a>一类的客户端，且已有代理服务器，可以打开V2rayU客户端菜单<code>服务器设置</code>选择你已有的服务器，选择<code>导入模式</code>，复制配置好的json。在<code>cat &gt; /etc/v2ray/config.json &lt;&lt; EOF</code> 命令后直接粘贴。</p>
</blockquote>

<pre><code class="language-text">sudo su - root
cat &gt; /etc/v2ray/config.json &lt;&lt; EOF
{
  &quot;inbounds&quot;: [{
    &quot;port&quot;: 1080,
    &quot;listen&quot;: &quot;127.0.0.1&quot;,
    &quot;protocol&quot;: &quot;socks&quot;,
    &quot;settings&quot;: {
      &quot;udp&quot;: true
    }
  }],
  &quot;outbounds&quot;: [{
    &quot;protocol&quot;: &quot;vmess&quot;,
    &quot;settings&quot;: {
      &quot;vnext&quot;: [{
        &quot;address&quot;: &quot;server ip&quot;,
        &quot;port&quot;: 10086,
        &quot;users&quot;: [{ &quot;id&quot;: &quot;b831381d-6324-4d53-ad4f-8cda48b30811&quot; }]
      }]
    }
  },{
    &quot;protocol&quot;: &quot;freedom&quot;,
    &quot;tag&quot;: &quot;direct&quot;,
    &quot;settings&quot;: {}
  }],
  &quot;routing&quot;: {
    &quot;domainStrategy&quot;: &quot;IPOnDemand&quot;,
    &quot;rules&quot;: [{
      &quot;type&quot;: &quot;field&quot;,
      &quot;ip&quot;: [&quot;geoip:private&quot;],
      &quot;outboundTag&quot;: &quot;direct&quot;
    }]
  }
}
EOF


# address改为服务端ip， port改为服务端端口, id改为服务端用户ID
</code></pre>

<h2 id="toc_2">开启v2ray服务</h2>

<pre><code class="language-text">sudo systemctl start v2ray

#查看v2ray服务状态, Active: active (running)代表程序运行中
sudo systemctl status v2ray

 v2ray.service - V2Ray Service
   Loaded: loaded (/etc/systemd/system/v2ray.service; enabled; vendor preset: enabled)
   Active: active (running) since Thu 2020-07-02 17:12:24 CST; 57min ago
     Docs: https://www.v2ray.com/
           https://www.v2fly.org/
 Main PID: 1303 (v2ray)
    Tasks: 9 (limit: 2200)
   Memory: 6.9M
   CGroup: /system.slice/v2ray.service
           └─1303 /usr/bin/v2ray/v2ray -config /etc/v2ray/config.json
</code></pre>

<h2 id="toc_3">使用ProxyChains-NG配置全局代理</h2>

<pre><code class="language-text">git clone https://github.com/rofl0r/proxychains-ng.git
cd /home/pi/proxychains-ng/
./configure &amp;&amp; make &amp;&amp; make install
</code></pre>

<p>添加配置:</p>

<pre><code class="language-text">cp ./src/proxychains.conf /etc/proxychains.conf

vim /etc/proxychains.conf

# 将最后的 socks4 127.0.0.1 9095 改为 socks5  127.0.0.1 1080
</code></pre>

<p>在需要使用代理的命令前面加上proxychains4, 如下</p>

<pre><code class="language-text">curl ip.sb
xxx.xxx.xxx.xxx
# 这里应该是你当前的ip

proxychains4 curl ip.sb
x.x.x.x
# 这里是代理的ip
</code></pre>

<p>Chrome浏览器使用代理</p>

<pre><code class="language-text">1. 首先通过命令行启动Chrome浏览器

chromium-browser --proxy-server=&quot;https=socks5://127.0.0.1:1080;http=socks5://127.0.0.1:1080&quot;

注：这种方式不可以在SSH中启动，因SSH中无界面导致失败。
</code></pre>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[树莓派环境安装]]></title>
    <link href="https://dsencheng.github.io/15936592764846.html"/>
    <updated>2020-07-02T11:07:56+08:00</updated>
    <id>https://dsencheng.github.io/15936592764846.html</id>
    <content type="html"><![CDATA[
<p><a href="https://installvirtual.com/how-to-install-python-3-8-on-raspberry-pi-raspbian/">如何在RaspberryPi上安装Python 3.8</a></p>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[FRP 笔记]]></title>
    <link href="https://dsencheng.github.io/15934342416600.html"/>
    <updated>2020-06-29T20:37:21+08:00</updated>
    <id>https://dsencheng.github.io/15934342416600.html</id>
    <content type="html"><![CDATA[
<p><a href="https://github.com/fatedier/frp/blob/master/README_zh.md">中文文档</a></p>

<span id="more"></span><!-- more -->

<h4 id="toc_0">1.下载</h4>

<pre><code class="language-text"># 树莓派需要对应的版本
wget https://github.com/fatedier/frp/releases/download/v0.33.0/frp_0.33.0_linux_amd64.tar.gz
</code></pre>

<h4 id="toc_1">2.解压缩</h4>

<pre><code class="language-text">tar -zxvf frp_0.33.0_linux_amd64.tar.gz
</code></pre>

<h4 id="toc_2">3.修改文件夹名字</h4>

<pre><code class="language-text">mv frp_0.33.0_linux_amd64 frp
</code></pre>

<h4 id="toc_3">3.1 或者复制一份目录及文件</h4>

<pre><code class="language-text">cp frp_0.33.0_linux_amd64 frp
</code></pre>

<h4 id="toc_4">4.进入目录开始操作</h4>

<pre><code class="language-text">cd frp
</code></pre>

<h1 id="toc_5">通过 ssh 访问内网机器</h1>

<p>1.将对应版本的 frps 及 frps.ini 放到具有公网 IP 的机器上。<br/>
2.修改 frps.ini 文件</p>

<pre><code class="language-text"># frps.ini
[common]
bind_port = 7000
</code></pre>

<p>3.启动 frps</p>

<pre><code class="language-text">./frps -c ./frps.ini
</code></pre>

<p>4.将 frpc 及 frpc.ini 放到处于内网环境的机器上。<br/>
5.修改 frpc.ini 文件 ，假设 frps 所在服务器的公网 IP 为 x.x.x.x</p>

<pre><code class="language-text"># frpc.ini
[common]
server_addr = x.x.x.x
server_port = 7000

[ssh]
type = tcp
local_ip = 127.0.0.1
local_port = 22
remote_port = 6000
</code></pre>

<p>6.启动 frpc </p>

<pre><code class="language-text">./frpc -c ./frpc.ini
</code></pre>

<p>7.通过 ssh 访问内网机器</p>

<pre><code class="language-text">ssh login_name@x.x.x.x -p 6000
</code></pre>

<h1 id="toc_6">开机启动frpc服务，同样可以启动frps服务【注意路径和文件名】。</h1>

<pre><code class="language-text">sudo vim /etc/systemd/system/frpc.service
</code></pre>

<p>按如下修改</p>

<pre><code class="language-text">[Unit]
Description=frpc daemon
After=syslog.target  network.target
Wants=network.target

[Service]
Type=simple
ExecStart=/usr/sbin/frp/frpc -c /etc/frp/frpc.ini
Restart= always
RestartSec=1min
ExecStop=/usr/bin/killall frpc


[Install]
WantedBy=multi-user.target
</code></pre>

<p>使用<code>sudo systemctl enable frpc.service</code>启用</p>

<pre><code class="language-text">//启动命令
sudo systemctl start frpc
//停止命令
sudo systemctl stop frpc
//重启命令
sudo systemctl restart frpc
//查看状态
sudo systemctl status frpc
</code></pre>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Ruby 笔记]]></title>
    <link href="https://dsencheng.github.io/15933946041998.html"/>
    <updated>2020-06-29T09:36:44+08:00</updated>
    <id>https://dsencheng.github.io/15933946041998.html</id>
    <content type="html"><![CDATA[
<p>内部使用外部传入的参数</p>

<pre><code class="language-text">xxx do |options|
    desc = options[:m]
    puts desc
    
调用时

xxx m:&quot;hello&quot;

</code></pre>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Xcode命令]]></title>
    <link href="https://dsencheng.github.io/15923835113038.html"/>
    <updated>2020-06-17T16:45:11+08:00</updated>
    <id>https://dsencheng.github.io/15923835113038.html</id>
    <content type="html"><![CDATA[
<p>命令行-单元测试</p>

<pre><code class="language-text">使用Cocoapods有workspace 必须加-workspace，注意使用引号及完整的文件名， [-only-testing:&lt;test-identifier&gt;] test-identifier格式说明 TestTarget[/TestClass[/TestMethod]]

xcodebuild test -workspace &#39;opensdk.xcworkspace&#39; -scheme &#39;opensdk&#39; -destination &#39;platform=iOS Simulator,name=iPhone 8,OS=13.5&#39;
xcodebuild test -workspace &#39;opensdk.xcworkspace&#39; -scheme &#39;opensdk&#39; -destination &#39;platform=iOS Simulator,name=iPhone 8,OS=13.5&#39; -only-testing:&#39;opensdkTests&#39;
xcodebuild test -workspace &#39;opensdk.xcworkspace&#39; -scheme &#39;opensdk&#39; -destination &#39;platform=iOS Simulator,name=iPhone 8,OS=13.5&#39; -only-testing:&#39;opensdkTests/LoginTests/testLoginExample&#39;

省略了class
xcodebuild test -workspace &#39;opensdk.xcworkspace&#39; -scheme &#39;opensdk&#39; -destination &#39;platform=iOS Simulator,name=iPhone 8,OS=13.5&#39; -only-testing:&#39;opensdkTests/testLoginExample&#39;

</code></pre>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Shell 笔记]]></title>
    <link href="https://dsencheng.github.io/15923777936483.html"/>
    <updated>2020-06-17T15:09:53+08:00</updated>
    <id>https://dsencheng.github.io/15923777936483.html</id>
    <content type="html"><![CDATA[
<p>获取podspec版本号</p>

<pre><code class="language-text">VersionString=`grep -E &#39;s.version.*=&#39; xxx.podspec`
VersionString=${VersionString##*=}
VersionNumber=`tr -d &quot;&#39;&quot; &lt;&lt;&lt;&quot;$VersionString&quot;`
echo &quot;current version is ${VersionNumber}&quot;
</code></pre>

<span id="more"></span><!-- more -->

<p>经过几天的Jenkins折腾，借助管道，获取版本号的命令现在仅需要一行.<br/>
首先使用<code>grep</code>命令，读取文件内容并过滤到<code>s.version</code>这一行；然后使用<code>tr</code>命令去掉单引号、字母、空格、等号；最后使用<code>sed</code>命令，替换第一个点。</p>

<pre><code class="language-text">grep -E &#39;s.version.*=&#39; xxx/xxx.podspec | tr -d &quot;&#39;a-z= &quot; | sed &quot;s/\.//1&quot;

3.3.3
</code></pre>

<p>Jenkins iOS修改版本号脚本示例。<br/>
iOS这里使用了不同的文件来区分环境，比较简单</p>

<pre><code class="language-text">sed -i &#39;&#39; -e &quot;s/CFBundleVersion.*/CFBundleVersion=2.33/g&quot; dev.xcconfig
</code></pre>

<p>Jenkins安卓修改版本号脚本示例。<br/>
安卓的不同环境是放在同一个文件里做区分，逻辑比较复杂。</p>

<pre><code class="language-text">#获取关键字行数    
rows=$(sed -n -e &#39;/isDev/=&#39; build.gradle)
#获取替换行数差值
di=$(grep -n -A 6 &#39;isDev{&#39; build.gradle | sed -n -e &#39;/versionCode/=&#39;)
#计算指定行数 
target=`expr $rows + $di - 1`
#替换指定行的内容
sed -n &quot;${target} s/versionCode .*/versionCode 99/p&quot; build.gradle
</code></pre>

<h4 id="toc_0">Sed命令</h4>

<ul>
<li><p>替换原始文件内容。<br/>
Mac系统不可以直接使用<code>-i</code>，改成<code>-i &#39;&#39; -e</code>的组合方式。<br/>
搜索<code>APP_Version</code>开头(包含)之后的任意字符,到换行符前。<br/>
替换为<code>APP=88.88</code>。<br/>
修改的文件<code>aa.txt</code>。批量替换可改为<code>*.txt</code>。</p>
<pre><code class="language-text">sed -i &#39;&#39; -e &#39;s/APP_Version.*/APP=88.88/g&#39; aa.txt
</code></pre></li>
<li><p>打印匹配内容的行号</p>
<pre><code class="language-text">sed -n -e &#39;/isPro/=&#39; build.gradle
</code></pre></li>
<li><p>正则截取/替换内容<br/>
1.假设变量<code>branch=branches/v0.0.9E2</code>，输出<code>v0.0.9E2</code>。</p>
<pre><code class="language-text">abc=`echo $branch | sed &#39;s/.*\///g&#39;`
echo $abc
</code></pre></li>
</ul>

<h4 id="toc_1">SCP命令</h4>

<p>远程复制文件</p>

<pre><code class="language-text">scp zzjs@172.16.77.210:~/Desktop/a.file ~/Desktop
scp ~/Desktop/a.file zzjs@172.16.77.210:~/Desktop
</code></pre>

<h4 id="toc_2">TR命令</h4>

<p>删除数字</p>

<pre><code class="language-text">echo &quot;hello 123 world 45345&quot; |tr -d &#39;0-9&#39;
</code></pre>

<p>保留数子</p>

<pre><code class="language-text">echo &quot;hello 123 world 45345&quot; |tr -dc  &#39;0-9&#39;
</code></pre>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Cocoapods之`pod`命令]]></title>
    <link href="https://dsencheng.github.io/15747536824820.html"/>
    <updated>2019-11-26T15:34:42+08:00</updated>
    <id>https://dsencheng.github.io/15747536824820.html</id>
    <content type="html"><![CDATA[
<p><code>pod init</code><br/>
初始化</p>

<p><code>pod install</code><br/>
安装/更新库</p>

<p><code>pod lib lint</code><br/>
验证lib合法性</p>

<span id="more"></span><!-- more -->

<p><code>pod lib lint --verbose --use-libraries --allow-warnings -- sources=&#39;私有仓库repo地址,https://github.com/CocoaPods/Specs&#39;</code><br/>
验证多仓库lib</p>

<p><code>pod spec lint</code><br/>
验证spec合法性</p>

<p><code>pod spec lint --verbose --use-libraries --allow-warnings -- sources=&#39;私有仓库repo地址,https://github.com/CocoaPods/Specs&#39;</code><br/>
验证多仓库spec</p>

<p><code>pod repo push DCSpecs name.podspec --use-libraries --allow-warnings</code><br/>
向DCSpecs推送DCServices库</p>

<p><code>pod repo push MCLib MCAppKit.podspec --verbose --use-libraries --allow-warnings</code><br/>
强制推送</p>

<p>以上命令均可选参数，<br/>
<code>--use-libraries</code>使用静态库<br/>
<code>--allow-warnings</code>允许警告</p>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Cocoapods之`Podfile`文件]]></title>
    <link href="https://dsencheng.github.io/15747536460432.html"/>
    <updated>2019-11-26T15:34:06+08:00</updated>
    <id>https://dsencheng.github.io/15747536460432.html</id>
    <content type="html"><![CDATA[
<pre><code class="language-text">#swift为动态库
use_frameworks!
#最低版本号
platform :ios, &#39;9.0&#39;
#忽略库里的警告
inhibit_all_warnings!

target &#39;DCServices_Example&#39; do
  pod &#39;DCServices&#39;, :path =&gt; &#39;../&#39;

  target &#39;DCServices_Tests&#39; do
    inherit! :search_paths

    
  end
end
</code></pre>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Cocoapods之`.podspec`文件]]></title>
    <link href="https://dsencheng.github.io/15747477473465.html"/>
    <updated>2019-11-26T13:55:47+08:00</updated>
    <id>https://dsencheng.github.io/15747477473465.html</id>
    <content type="html"><![CDATA[
<pre><code class="language-text">#名字
s.name             = &#39;Chat2Love&#39;
#版本号，更新发布内容最好更新版本号
s.version          = &#39;0.1.0&#39;
#简介说明
s.summary          = &#39;A short description of Chat2Love.&#39;
#详细说明
s.description      = &#39;&#39;
#主页，通常是github项目地址
s.homepage         = &#39;https://github.com/dsencheng/Chat2Love&#39;
#屏幕快照链接
# s.screenshots     = &#39;www.example.com/screenshots_1&#39;, &#39;www.example.com/screenshots_2&#39;
#协议
s.license          = { :type =&gt; &#39;MIT&#39;, :file =&gt; &#39;LICENSE&#39; }
#作者
s.author           = { &#39;dsencheng&#39; =&gt; &#39;dsencheng@gmail.com&#39; }
#源码地址
s.source           = { :git =&gt; &#39;https://github.com/dsencheng/Chat2Love.git&#39;, :tag =&gt; s.version.to_s }
#社交信息
# s.social_media_url = &#39;https://twitter.com/&lt;TWITTER_USERNAME&gt;&#39;
#开发版本
s.ios.deployment_target = &#39;8.0&#39;
#暴露的文件，举例：`/*.h` 仅暴露.h文件。 `/*.{h,m} ` 仅暴露.h和.m文件
s.source_files = &#39;Chat2Love/Classes/**/*&#39;
#资源文件
# s.resource_bundles = {
#   &#39;Chat2Love&#39; =&gt; [&#39;Chat2Love/Assets/*.png&#39;]
# }
#暂未用到
# s.public_header_files = &#39;Pod/Classes/**/*.h&#39;
#需要使用到的系统库
# s.frameworks = &#39;UIKit&#39;, &#39;MapKit&#39;
# 需要依赖的三方库，支持指定版本,不支持指定地址。可在podfile文件中指定地址。
# s.dependency &#39;AFNetworking&#39;, &#39;~&gt; 2.3&#39;
#静态库
#s.static_framework = true
# 依赖的系统静态库
#s.library = &#39;c++&#39;, &#39;resolv&#39;
# build setting中的配置
#s.ios.pod_target_xcconfig = { &#39;OTHER_LDFLAGS&#39; =&gt; &#39;-lObjC&#39;, &#39;ENABLE_BITCODE&#39; =&gt; false, }
# 子库,会有不同的文件夹
#  s.subspec &#39;Sub&#39; do | b |
#      b.source_files = &#39;Chat2Love/Classes/**/*&#39;
#   end
# 引用其他的库，不建议样写，pod中引用framework文件会报错。
# s.ios.vendored_frameworks = &#39;Ali/Classes/AliRTCSdk.framework&#39;,&#39;Ali/Classes/UTDID.framework&#39;
# 引用其他的库，建议写法
#  s.subspec &#39;AliRTC&#39; do | sub_rtc |
#      sub_rtc.ios.vendored_frameworks = &#39;Ali/Classes/AliRTCSdk.framework&#39;,&#39;Ali/Classes/UTDID.framework&#39;
#   end

</code></pre>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Cocoapods之私有库发布及使用]]></title>
    <link href="https://dsencheng.github.io/15747423574103.html"/>
    <updated>2019-11-26T12:25:57+08:00</updated>
    <id>https://dsencheng.github.io/15747423574103.html</id>
    <content type="html"><![CDATA[
<p>本文以github为例<br/>
1、打开你的github主页，选择 <code>New repository</code> .</p>

<blockquote>
<p>a. <code>Repository name</code> 填写 <code>DCSpecs</code>。<br/>
b. <code>Description</code> 可以不填写。<br/>
c. 选择 <code>Private</code> 或 <code>Public</code>。<br/>
d. 勾选 <code>Initialize this repository with a README</code>。<br/>
e. 点击 <code>Create repository</code> 按钮。</p>
</blockquote>

<span id="more"></span><!-- more -->

<p>2、打开终端，进入目录，执行命令</p>

<pre><code class="language-text">pod repo add DCSpecs https://github.com/dsencheng/DCSpecs.git
</code></pre>

<p>3、执行成功后，可以进入目录查看 <code>~/.cocoapods/repos/</code> ，是否有<code>DCSpecs</code>文件夹。</p>

<p>4、在<code>DCServices.podspec</code>文件目录，推送私有库。</p>

<pre><code class="language-text">pod repo push DCSpecs DCServices.podspec
</code></pre>

<blockquote>
<p><code>DCServices</code>是<code>code</code>。<br/>
<code>DCSpecs</code> 是管理和记录。它会记录<code>DCServices</code>的<code>0.1.0</code>，<code>0.1.1</code> ... <code>1.5.3</code>。<br/>
写命令时，注意区分<code>DCServices</code> 和 <code>DCSpecs</code></p>
</blockquote>

<p>5、在项目中引入私有库。打开你开发的项目，如果没有<code>Podfile</code>，请先初始化。添加内容</p>

<pre><code class="language-text">source &#39;https://cdn.cocoapods.org/&#39;
source &#39;https://github.com/dsencheng/DCSpecs.git&#39;

platform :ios, ‘9.0’

target “your_target_name”   
    do
        pod &#39;DCServices&#39;
    end
</code></pre>

<p>打开终端在当前项目目录执行<code>pod install</code>完成!</p>

]]></content>
  </entry>
  
</feed>
